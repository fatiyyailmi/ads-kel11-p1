import { Search } from "lucide-react";

const SearchBar = ({
    placeholder = "Cari...",
    value,
    onChange,
}) => {

    return (
        <div className ="
            flex items-center
            bg-indigo-50
        ">

        </div>
    )
}